export enum FragmentType {
    CONSTRUCTOR = "constructor",
    FALLBACK = "fallback",
    FUNCTION = "function",
    EVENT = "event",
    ERROR = "error",
}

export enum FragmentStateMutability {
    PURE = "pure",
    VIEW = "view",
    PAYABLE = "payable",
    NON_PAYABLE = "nonpayable",
}
